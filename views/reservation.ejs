<div id="reservation"  data-backdrop="true"  class="modal fade" style="font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif, Cochin, Georgia, Times, 'Times New Roman', sans-serif">
    <div class="modal-dialog " >
        <div class="modal-content ">
            <div class="modal-header">
                <button type="button"  class="close" data-dismiss="modal">x</button>
            </div>
            <div class="modal-body">
                <form class="form" action="/reservation" method="post">
                    <div class="form-group">
                        <% if (locals.utilisateur.user == 0) var read = "readonly" %>
                        <input type="text" step="1" id="cin" value= "<%=locals.utilisateur.cinClient %>"  name="cin" placeholder="C.I.N" class="form-control input-lg" required <%= read %> />
                    </div>
                    <div class="form-group">
                        <input type="date" min="" id="daty" value="" name="date" class="form-control input-lg" required/>
                    </div>
                    <div class="form-group">
                        <input type="number" min="5" max="200" id="nbre" name="effectif" placeholder="Nombres de personnes (min:5 max:200)"  class="form-control input-lg" required/>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Réserver" class="btn btn-info col-xs-6" />
                        <input type="reset" class="btn btn-danger col-xs-6" />
                    </div>
                </form>
            </div>
            <div class="modal-footer" style="margin-top: 30px">
                <button class="btn btn-primary col-xs-12"  data-dismiss="modal" >Annuler</button>
            </div>
        </div>
    </div>
</div>

